<!-- 
<% shortLinkHelper = (link) => {
    let arr_link =  link.split(" ")
    let short_link = ''
    if (arr_link.length >1) {
        short_link = arr_link[0] + ' ' + arr_link[1] + ' ' + arr_link[2] + '[...]'
    } else {
        short_link = arr_link[0]
    }

    return short_link
}

%> -->